kind: llman.sdd.delta
ops[1]{op,req_id,title,statement,from,to,name}:
  add_requirement,r14,owns_daemon Drop 关闭,DaemonClient 在 owns_daemon=true 被 Drop 时 MUST 尽力发送 daemon/shutdown（或等价终止），不得仅依赖 10min idle TTL。,null,null,null
op_scenarios[1]{req_id,id,given,when,then}:
  r14,"drop-shutdown","MCP 客户端 auto-start 了 daemon",客户端 Drop,向 daemon 发出 shutdown 请求

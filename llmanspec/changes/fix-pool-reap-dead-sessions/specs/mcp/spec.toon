kind: llman.sdd.delta
ops[1]{op,req_id,title,statement,from,to,name}:
  add_requirement,r13,回收已死 LSP 子进程,LspPool 在查找/复用会话前 MUST 检测子进程已退出（try_wait），并移除死会话以便重新 spawn，不得一直复用到 idle reaper。,null,null,null
op_scenarios[1]{req_id,id,given,when,then}:
  r13,"reap-dead",池中会话的 LSP 子进程已退出,再次 get_or_spawn 同 key,旧会话被移除并创建新会话
kind: llman.sdd.spec
name: transport
purpose: "传输层抽象，定义所有传输层必须实现的 I/O trait，支持 stdio、TCP、WebSocket 等协议。"
valid_scope[2]: src/transport/,tests/
requirements[4]{req_id,title,statement}:
  r1,Transport trait,系统 MUST 定义 Transport trait 包含 receive 和 send 方法。
  r2,Content-Length 分帧,所有传输层 MUST 处理 Content-Length 分帧协议。
  r3,多协议支持,系统 MUST 支持 StdioTransport、TcpTransport、WsTransport 三种传输协议。
  r4,进程退出检测,Transport trait MUST 提供 try_wait 方法检测底层进程退出状态。
scenarios[7]{req_id,id,given,when,then}:
  r1,stdio-transport,"StdioTransport 实例",调用 receive(),返回 Content-Length 分帧的消息
  r1,tcp-transport,"TcpTransport 实例",调用 receive(),返回 Content-Length 分帧的消息
  r1,ws-transport,"WsTransport 实例",调用 receive(),返回 Content-Length 分帧的消息
  r2,content-length-raw,"原始 LSP 消息",发送前,自动添加 Content-Length 头
  r2,content-length-parse,"带 Content-Length 的消息",接收时,自动解析并提取消息体
  r3,multiple-protocols,"配置使用不同传输协议",Proxy 创建,成功创建对应的 Transport 实例
  r4,process-exit,"子进程已退出",调用 try_wait(),返回 Some(ExitStatus)

kind: llman.sdd.spec
name: lsp-compatibility
purpose: "LSP 兼容性规范，确保输入侧完全符合 LSP 3.17 规范，输出侧以 token 效率优先。"
valid_scope[2]: src/proxy.rs,src/transport/
requirements[5]{req_id,title,statement}:
  r1,输入兼容,系统 MUST 完全符合 LSP 3.17 规范处理 Server→lspz 的输入。
  r2,输出自由,系统输出（lspz→Agent）MUST 不受 LSP 标准约束，以 token 效率优先。
  r3,透明转发,非目标消息 MUST 透明转发，不干扰 LSP 通信流。
  r4,能力协商,系统 MUST 正确处理 server/client capabilities，不做篡改。
  r5,错误隔离,压缩失败 MUST 不影响 LSP 通信。
scenarios[7]{req_id,id,given,when,then}:
  r1,lsp-317-input,"LSP 3.17 服务器响应",Proxy 接收,正确解析符合规范
  r2,toon-output,"诊断数据",压缩后输出,使用 TOON 格式而非标准 LSP JSON
  r3,forward-non-target,"非诊断消息",Proxy 接收,透明转发给客户端
  r3,forward-initialize,"initialize 请求",Proxy 接收,透明转发给服务器
  r4,capabilities-forward,"server capabilities",Proxy 接收,原样转发给客户端
  r4,capabilities-no-modify,"client capabilities",Proxy 接收,不修改直接转发
  r5,compression-fail,"压缩器异常",InterceptorChain 处理,返回原始消息且 LSP 通信继续
